package v1

import (
	"net/http"
)

func (rr RoomsResource) NewRoom(w http.ResponseWriter, r *http.Request) {
	//render.JSON(w, r, VersionResponse{
	//	API:     APIVersion,
	//	Version: vr.Version,
	//})
}
